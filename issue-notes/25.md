# issue self updateに失敗した #25
[issues #25](https://github.com/cat2151/voicevox-playground-tui/issues/25)


# 表でupdateした場合のエラー

```
    Replaced package `voicevox-playground-tui v0.1.0 (https://github.com/cat2151/voicevox-playground-tui#80b1b305)` with `voicevox-playground-tui v0.1.0 (https://github.com/cat2151/voicevox-playground-tui#faa554b0)` (executable `vpt.exe`)
バッチ ファイルが見つかりません。
```

# 裏でupdateした場合もエラーになっていた（exeがlockされていてそのまま落ちた、という表示だった）

# userがほしいもの
- windowsで、バッチファイルを利用して、表（起動時にforegroundでself update）のupdateを、まず成功までもっていってほしい
